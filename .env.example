# AI News Analysis Layer Configuration
# When set, the system connects directly to Google Gemini API (gemini-3.6-flash)
# Server-side only; never exposed to the frontend or SQLite.
GEMINI_API_KEY=
AI_PROVIDER=gemini
AI_MODEL=gemini-3.6-flash
